#include "main.h"
/**
 * _abs - Prints the absolute value of an integer
 * @r: An input number
 *
 *Return: An absolute value of number n
 */
int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (r * -1);
}
